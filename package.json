{
  "name": "tauri-local-first",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "packageManager": "pnpm@9.0.0",
  "scripts": {
    "build": "pnpm -r run build",
    "dev:launcher": "pnpm --filter launcher tauri dev",
    "build:launcher": "pnpm --filter launcher tauri build"
  }
}
